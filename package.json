{
  "name": "abipulli-types",
  "version": "1.0.2",
  "description": "Shared TypeScript types for frontend and backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "publish": "npm version patch && npm publish",
    "git-push": "git add . && git commit -m 'publish types' && git push",
    "push": "npm run prepare && npm run publish && npm run git-push"
  },
  "files": [
    "dist"
  ],
  "author": "Leif Etter",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
